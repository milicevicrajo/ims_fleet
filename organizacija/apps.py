from django.apps import AppConfig


class OrganizacijaConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "organizacija"
    verbose_name = "Centralni registar organizacije"
